version: "3"

services:
  frontend: 
    build: ./frontend 

    ports: 
      - "8081:80"

  backend:
    build: ./backend 

    ports:
      - "5000:5000"    

  networks:
    app-network:
      driver: bridge
   
         
